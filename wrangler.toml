# Cloudflare Workers 部署配置（纯 API，不含前端静态资源）
# 前端由 GitHub Pages 托管：https://steamaa1.github.io/llm-chess/

name = "llm-chess-api"
main = "apps/api/src/worker.ts"
compatibility_date = "2025-07-31"
compatibility_flags = ["nodejs_compat"]

# 纯 API Worker —— 前端部署在 GitHub Pages
